// enemies.js — slime / archer / wisp. Stubbed; real logic in later commits.

const Enemies = (() => {
  function update(list, dt, level, player) {}
  function draw(ctx, list, camera) {}
  return { update, draw };
})();
